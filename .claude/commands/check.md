# בדיקה עצמית

הרץ את הפקודות הבאות לפי הסדר ותקן כל שגיאה לפני שתמשיך:

1. `npm run typecheck` — TypeScript errors
2. `npm run lint` — Lint errors
3. אם יש שגיאות, תקן אותן עכשיו לפני שתמשיך בכל משימה אחרת.

דווח: כמה שגיאות היו, כמה תוקנו.
