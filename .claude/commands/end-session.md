# סיום סשן

## שלב 1 — עדכן progress
1. סמן `[ ]` שהושלמו כ-`[x]` ב-docs/progress.md
2. תחת "📋 הבא בתור" — כתוב משפט אחד: מה הצעד הראשון לסשן הבא
3. הוסף כל בעיה חדשה תחת "🐛 באגים ידועים"

## שלב 2 — נתח שגיאות חוזרות
קרא את הקובץ `.claude/error-patterns.log` אם קיים.
חפש שגיאות שמופיעות 2+ פעמים בסשן הנוכחי.

אם מצאת שגיאות חוזרות — הצג לי:
```
🔄 שגיאה חוזרת: [תיאור]
הסיבה: [למה זה קורה]
הצעה ל-CLAUDE.md: "[חוק חדש מוצע]"
להוסיף? (כן/לא)
```
המתן לתשובתי לפני שנוגעים ב-CLAUDE.md.

## שלב 3 — git commit
הרץ: `git add docs/progress.md && git commit -m "chore: session progress update"`

## שלב 4 — סיכום
הדפס 3 שורות:
- ✅ הושלם:
- 🔄 בעבודה:
- ➡️ הפעם הבאה:
