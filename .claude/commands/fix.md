# תיקון בעיה: $ARGUMENTS

1. הרץ את הפקודה/בדיקה שמראה את הבעיה
2. הפעל explorer subagent להבין את הקוד הרלוונטי
3. תקן — שינויים קטנים וממוקדים בלבד
4. הרץ `npm run typecheck && npm run lint`
5. הרץ שוב את הפקודה המקורית לוודא שתוקן

אל תגע בקוד שלא קשור לבעיה.
